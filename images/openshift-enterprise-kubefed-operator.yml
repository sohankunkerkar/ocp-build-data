content:
  source:
    dockerfile: Dockerfile
    git:
      branch:
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/kubefed.git
    path: operator
from:
  member: openshift-enterprise-base
name: openshift/kubefed-operator
owners:
- kubefed-team@redhat.com
update-csv:
  manifests-dir: deploy/olm-catalog/kubefed-operator-manifests/
  registry: image-registry.openshift-image-registry.svc:5000
mode: disabled
